package testbench.gradle

/*
 * This extension has a few goals:
 * - Make the Desktop App runnable by downloading a shadow jar from maven
 * - Search for custom plugin modules and build/include them when running the Desktop app
 * - Allow customization of included plugins when running the Desktop app
 */
public class TestBenchExtension
